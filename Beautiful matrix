#include<bits/stdc++.h>
using namespace std;
int main()
{
   int m,n;
   for(int i=1;i<6;i++)
   {
      for(int j=1;j<6;j++)
      {
          cin>>m;
          if(m==1)
            n=abs(i-3) + abs(j-3);
      }
   }
   cout<<n;
        return 0;
}
