#include<bits/stdc++.h>
using namespace std;
int main()
{
   int n,a,h,c=0;
   cin>>n>>h;
   for(int i=0;i<n;i++)
   {
       cin>>a;
       if(a>h)
       {
           c=c+2;
       }
       else
        c++;
   }
   cout<<c;

    return 0;
}
