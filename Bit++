#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,a=0;
    cin>>n;
    string s;
    for(int i=0;i<n;i++)
    {
        cin>>s;
        if(s=="++X" ||s=="X++")
            a++;

    else
        a--;
    }
    cout<<a;

        return 0;
}
