#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,a[100],sum=0;
    cin>>n;
    for(int i=0;i<n;i++)
    {
        cin>>a[i];
        if(a[i]==1)
          sum++;
    }
if(sum!=0)
    cout<<"HARD";
else
    cout<<"EASY";
return 0;

}
